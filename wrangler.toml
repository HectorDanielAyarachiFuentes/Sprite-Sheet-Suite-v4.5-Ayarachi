# wrangler.toml
name = "sprite-sheet-1"
main = "index.js"
compatibility_date = "2025-09-24"

# Indica a Wrangler que el directorio actual contiene los archivos estáticos
# que se subirán a un bucket de KV para ser servidos.
[site]
bucket = "."

[build]
command = "npm install"

# Añade esta sección para vincular el secreto
[[vars]]
CLERK_SECRET_KEY = "CLERK_SECRET_KEY"
